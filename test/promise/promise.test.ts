describe('Promise', () => {
    let systemUnderTest: Function = getUserDetails;

    describe('when requested for user profile', () => {
        it('should have dateOfBirth with type `Date`', () => {
        });
    });
});
